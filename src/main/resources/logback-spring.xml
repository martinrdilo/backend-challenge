<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
        Structured JSON logging via logstash-logback-encoder.
        Profile-aware: human-readable text for dev/default, JSON for prod.
        MDC fields (correlationId, userId, channel, notificationId) appear
        as top-level JSON keys when set.
    -->

    <!-- ============ Console Appender (text — dev/default profile) ============ -->
    <springProfile name="default,dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%mdc] - %msg%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <!-- ============ Console Appender (JSON — non-dev/default profiles) ============ -->
    <springProfile name="!default &amp; !dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <fieldName>timestamp</fieldName>
                    </timestamp>
                    <logLevel>
                        <fieldName>level</fieldName>
                    </logLevel>
                    <loggerName>
                        <fieldName>logger</fieldName>
                    </loggerName>
                    <threadName>
                        <fieldName>thread</fieldName>
                    </threadName>
                    <message>
                        <fieldName>message</fieldName>
                    </message>
                    <stackTrace>
                        <fieldName>exception</fieldName>
                        <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                            <maxDepthPerThrowable>30</maxDepthPerThrowable>
                            <maxLength>2048</maxLength>
                            <shortenedClassNameLength>20</shortenedClassNameLength>
                            <rootCauseFirst>true</rootCauseFirst>
                        </throwableConverter>
                    </stackTrace>
                    <mdc>
                        <fieldName>correlationId</fieldName>
                    </mdc>
                    <mdc>
                        <fieldName>userId</fieldName>
                    </mdc>
                    <mdc>
                        <fieldName>channel</fieldName>
                    </mdc>
                    <mdc>
                        <fieldName>notificationId</fieldName>
                    </mdc>
                </providers>
            </encoder>
        </appender>
    </springProfile>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
